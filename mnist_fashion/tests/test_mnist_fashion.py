# note: pytest requires that .py files containing tests are named by the test_* pattern
#       names of test methods within these files have to start with test_


def test_if_testing_harness_works():
    """Tests if the testing harness works in general."""
    assert True
